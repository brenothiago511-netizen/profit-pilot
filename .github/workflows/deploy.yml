name: Deploy to Vercel

# Desabilitado - Vercel já faz deploy automático via GitHub integration
on:
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Deploy gerenciado automaticamente pelo Vercel GitHub Integration"
